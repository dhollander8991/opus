//I would ideally use Commander 
//and allow the user to change the number from CLI
//Alternativly Add a web UI allowing the user to set the number of tests

const testcases = Array.from({ length: 10 }, (v, i) => i);

module.exports = testcases;